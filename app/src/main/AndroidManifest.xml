<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/adaptive_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Billmate"
        tools:targetApi="tiramisu">
        <activity
            android:name=".activitys.editItems.EditFunds"
            android:exported="false"
            android:label="@string/title_activity_edit_funds"
            android:theme="@style/Theme.Billmate" />
        <activity
            android:name=".activitys.editItems.EditTransfers"
            android:exported="false" />
        <activity
            android:name=".activitys.editItems.EditEvents"
            android:exported="false" />
        <activity
            android:name=".activitys.newItems.CreateNewCategory"
            android:exported="false" />
        <activity
            android:name=".activitys.newItems.CreateNewEvents"
            android:exported="false" />
        <activity
            android:name=".activitys.newItems.CreateNewFund"
            android:exported="false" />
        <activity
            android:name=".activitys.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Billmate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>