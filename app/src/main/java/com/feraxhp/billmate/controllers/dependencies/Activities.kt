package com.feraxhp.billmate.controllers.dependencies

enum class Activities {
    main,
    createNewCategory,
    createNewEvents,
    createNewFund,
    editEvents,
    editTransfers,
    editFunds,
    editCategories
}